name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Buildzer
      - uses: ArtemSerebrennkov/buildozer-action@v1      


        with:
          workdir: 
